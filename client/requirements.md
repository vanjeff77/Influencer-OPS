## Packages
recharts | Dashboard charts for finance and tracking
framer-motion | Smooth transitions for modals and sidebars
date-fns | Date formatting for campaigns and emails
lucide-react | Icons for the sidebar and actions (already in base but good to note)
clsx | Class name utility (already in base)
tailwind-merge | Class name utility (already in base)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
